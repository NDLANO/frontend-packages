import { Canvas, Meta, Story, ArgsTable } from '@storybook/addon-docs';
import { defaultParameters } from '../../designmanual/stories/defaults';

import ButtonV2 from './ButtonV2';

<Meta
  title="Enkle komponenter/Button"
  component={ButtonV2}
  parameters={{
    ...defaultParameters,
    docs: {
      inlineStories: true,
    },
    backgrounds: {},
  }}
/>

export const Template = (args) => <ButtonV2 {...args} />;

# Knapp

<Canvas>
  <Story
    name="Normal"
    args={{
      colorTheme: 'primary',
      children: 'Button',
      size: 'normal',
      variant: 'solid',
      shape: 'normal',
      fontWeight: 'normal',
    }}>
    {Template.bind({})}
  </Story>
</Canvas>

### Props

<ArgsTable story="Normal" />

# Varianter

## Ghost knapp

<Canvas>
  <Story
    name="Ghost"
    args={{
      colorTheme: 'light',
      children: 'Button',
      size: 'normal',
      variant: 'ghost',
      shape: 'pill',
      fontWeight: 'normal',
    }}>
    {Template.bind({})}
  </Story>
</Canvas>

## Outline

<Canvas>
  <Story
    name="Outline"
    args={{
      colorTheme: 'primary',
      children: 'Button',
      size: 'normal',
      variant: 'outline',
      shape: 'normal',
      fontWeight: 'normal',
    }}>
    {Template.bind({})}
  </Story>
</Canvas>

## Link

<Canvas>
  <Story
    name="Link"
    args={{
      colorTheme: 'primary',
      children: 'Link',
      size: 'normal',
      variant: 'link',
      shape: 'normal',
      fontWeight: 'normal',
    }}>
    {Template.bind({})}
  </Story>
</Canvas>
