import { Canvas, Meta, Story, ArgsTable } from '@storybook/addon-docs';
import { defaultParameters } from '../../../../stories/defaults';
import { Pencil } from '@ndla/icons/action';
import { DeleteForever } from '@ndla/icons/editor';

import Codeblock from './Codeblock';

<Meta
  title="Enkle komponenter/Kodeblokk"
  component={Codeblock}
  parameters={{
    ...defaultParameters,
    docs: {
      inlineStories: true,
    },
    backgrounds: {},
  }}
/>

export const Template = (args) => <Codeblock {...args} />;

# Standard ressurs

<Canvas>
  <Story
    name="Codeblock"
    args={{
      title: 'Tittel til ressurs',
      code: `<div>
  <h1>Hva er Lorem Ipsum?</h1>
  <p><span>Lorem Ipsum</span> er rett og slett dummytekst fra og for trykkeindustrien.</p>
</div>`,
      format: 'HMTL',
      showCopy: true,
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

### Props

<ArgsTable story="Codeblock" />
