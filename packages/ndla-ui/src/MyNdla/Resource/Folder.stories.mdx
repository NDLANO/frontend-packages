import { Canvas, Meta, Story, ArgsTable } from '@storybook/addon-docs';
import { defaultParameters } from '../../../../../stories/defaults';
import { Pencil } from '@ndla/icons/action';
import { DeleteForever } from '@ndla/icons/editor';
import { i18nInstance } from '@ndla/ui';
import { BrowserRouter } from 'react-router-dom';

import Folder from './Folder';

<Meta
  title="Enkle komponenter/Folder"
  component={Folder}
  parameters={{
    ...defaultParameters,
    docs: {
      inlineStories: true,
    },
    backgrounds: {},
  }}
  argTypes={{
    menuItems: {
      control: false,
    },
  }}
/>

export const Template = (args) => <Folder {...args} />;

# Folder

<Canvas>
  <Story
    name="Folder"
    args={{
      id: '123',
      title: 'Dette er min tittel',
      subFolders: 3,
      subResources: 3,
      link: '',
      type: 'list',
      menuItems: [
        { icon: <Pencil />, text: 'Rediger', onClick: () => {} },
        { icon: <DeleteForever />, text: 'Slett', onClick: () => {}, type: 'danger' },
      ],
    }}>
    {Template.bind({})}
  </Story>
</Canvas>

### Props

<ArgsTable story="Folder" />

# Varianter

## List

<Canvas>
  <BrowserRouter>
    <Folder title="Mappe 3" type="list" subFolders="3" subResources="3" />
  </BrowserRouter>
</Canvas>

## Block

<Canvas>
  <BrowserRouter>
    <Folder title="Tittel" type="block" subFolders="3" subResources="3" />
  </BrowserRouter>
</Canvas>
