import { Canvas, Meta, Story, ArgsTable } from '@storybook/addon-docs';
import { defaultParameters } from '../../../../../stories/defaults';

import FolderInput from './FolderInput';

<Meta
  title="Enkle komponenter/FolderInput"
  component={FolderInput}
  parameters={{
    ...defaultParameters,
    docs: {
      inlineStories: true,
    },
    backgrounds: {},
  }}
/>

export const Template = (args) => <FolderInput {...args} />;

# Ny mappe

<Canvas>
  <Story
    name="FolderInput"
    args={{
      labelHidden: true,
      onSave: () => console.log('onSave'),
      onClose: () => console.log('onClose'),
      label: '',
      name: '',
    }}>
    {Template.bind({})}
  </Story>
</Canvas>

### Props

<ArgsTable story="FolderInput" />

Alle props for et vanlig input-felt kan brukes med denne komponenten
