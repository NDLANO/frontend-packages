import { Canvas, Meta, Story, ArgsTable } from '@storybook/addon-docs';
import { defaultParameters } from '../../../../stories/defaults';
import { Pencil } from '@ndla/icons/action';
import { DeleteForever } from '@ndla/icons/editor';
import { BrowserRouter } from 'react-router-dom';

import BlockResource from './BlockResource';

export const menuItems = [
  { icon: <Pencil />, text: 'Rediger', onClick: () => {} },
  { icon: <DeleteForever />, text: 'Slett', onClick: () => {}, type: 'danger' },
];

export const tags = ['tag'];

export const resourceImage = {
  src: 'https://cdn.pixabay.com/photo/2022/06/12/22/35/village-7258991_1280.jpg',
  alt: '',
};

<Meta
  title="Enkle komponenter/Ressurser/BlockResource"
  component={BlockResource}
  parameters={{
    ...defaultParameters,
    docs: {
      inlineStories: true,
    },
    backgrounds: {},
  }}
  argTypes={{
    menuItems: {
      control: false,
    },
  }}
/>

export const Template = (args) => <BlockResource {...args} />;

# BlockResource

<Canvas>
  <Story
    name="BlockResource"
    args={{
      id: '1234',
      link: '',
      title: 'Tittel til ressurs',
      resourceImage: {
        src: '',
        alt: '',
      },
      resourceTypes: [{ id: 'urn:resourcetype:learningPath', name: 'Læringssti' }],
      tags: tags,
    }}>
    {Template.bind({})}
  </Story>
</Canvas>

### Props

<ArgsTable story="BlockResource" />

# Varianter

## Blokkressurs med bilde

<Canvas>
  <BrowserRouter>
    <BlockResource
      id="1234"
      title="Helt Vanlig Tittel"
      resourceTypes={[{ id: 'urn:resourcetype:subjectMaterial', name: 'Fagstoff' }]}
      tags={tags}
      description={'Dette er for eksempel en fagbeskrivelse! Dersom den er for lang vil den bli forkortet'}
      resourceImage={resourceImage}
      link={''}
      menuItems={menuItems}
    />
  </BrowserRouter>
</Canvas>

## Blokkressurs uten bilde

<Canvas>
  <BrowserRouter>
    <BlockResource
      id="1234"
      title="Helt Vanlig Tittel"
      resourceTypes={[{ id: 'urn:resourcetype:subjectMaterial', name: 'Fagstoff' }]}
      tags={tags}
      description={'Dette er for eksempel en fagbeskrivelse! Dersom den er for lang vil den bli forkortet'}
      resourceImage={{
        src: '',
        alt: '',
      }}
      link={''}
      menuItems={menuItems}
    />
  </BrowserRouter>
</Canvas>

#### Ulike ressurstyper:

- **Læringssti:** urn:resourcetype:learningPath
- **Fagstoff:** urn:resourcetype:subjectMaterial
- **Oppgaver og aktiviteter:** urn:resourcetype:tasksAndActivities
- **Vurderingsressurs:** urn:resourcetype:reviewResource
- **Ekstern læringsressurs:** urn:resourcetype:externalResource
- **Kildemateriale:** urn:resourcetype:SourceMaterial
