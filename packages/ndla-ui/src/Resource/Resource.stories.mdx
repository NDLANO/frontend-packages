import { Canvas, Meta, Story, ArgsTable } from '@storybook/addon-docs';
import { defaultParameters } from '../../../designmanual/stories/defaults';
import { Pencil } from '@ndla/icons/action';
import { DeleteForever } from '@ndla/icons/editor';

import ListResource from './ListResource';

<Meta
  title="Enkle komponenter/Resource"
  component={ListResource}
  parameters={{
    ...defaultParameters,
    docs: {
      inlineStories: true,
    },
    backgrounds: {},
  }}
/>

export const Template = (args) => <ListResource {...args} />;

# Mappe

<Canvas>
  <Story
    name="Resource"
    args={{
      id: '1234',
      link: '',
      title: 'Tittel til ressurs',
      resourceImage: {
        src: '',
        alt: '',
      },
      resourceTypes: [{ id: 'urn:resourcetype:learningPath', name: 'Læringssti' }],
    }}>
    {Template.bind({})}
  </Story>
</Canvas>

### Props

<ArgsTable story="Resource" />
