import { Canvas, Meta, Story, ArgsTable } from '@storybook/addon-docs';
import { defaultParameters } from '../../../designmanual/stories/defaults';
import Select from './Select';

<Meta
  title="Enkle komponenter/Select"
  component={Select}
  parameters={{
    ...defaultParameters,
    docs: {
      inlineStories: true,
    },
    backgrounds: {},
  }}
  argTypes={{
    value: { description: 'For controlled state' },
    onValueChange: { description: 'For controlled state' },
    prefix: { description: 'For controlled state' },
  }}
/>

export const Template = (args) => <Select {...args} />;

# Select

<Canvas>
  <Story
    name="Select"
    args={{
      label: 'Farger',
      selectElements: ['Gul', 'Blå', 'Grønn'],
      placeholder: 'Velg en farge',
    }}>
    {Template.bind({})}
  </Story>
</Canvas>

### Props

<ArgsTable story="Select" />
