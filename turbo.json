{
  "$schema": "https://turborepo.org/schema.json",
  "pipeline": {
    "build": {
      "dependsOn": ["^build"],
      "outputs": ["lib/**/*.js", "es/**/*.js"]
    },
    "prepublish": {
      "dependsOn": ["build"],
      "outputs": ["lib/**/*.d.ts"]
    }
  }
}
